<html>
<meta charset="UTF-8">
<head>
  <title>Aviso</title>
  <HTA:APPLICATION
      ID="AvisoEmpresa"
      APPLICATIONNAME="AvisoEmpresa"
      BORDER="none"
      CAPTION="no"
      SHOWINTASKBAR="no"
      SINGLEINSTANCE="yes"
      SYSMENU="no"
      WINDOWSTATE="normal"/>
  <style>
    body {
      margin: 0;
      border: 0;
      background-color: black;
      overflow: hidden;
      font-family: Segoe UI, sans-serif;
    }
    #toast {
      padding: 0 0 0 10%;
      color: white;
      border-radius: 16px;
      animation: slidein 0.6s ease-out forwards;
    }
    #toast h2 {
      font-size: 23px;
      margin: 0 0 5px 0;
      font-weight: 600;
    }
    #toast p {
      margin: 0 0 10px 0;
      font-size: 15px;
    }
    @keyframes slidein {
      from { opacity: 0; transform: translateY(50px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes slideout {
      from { opacity: 1; transform: translateY(0); }
      to   { opacity: 0; transform: translateY(50px); }
    }
    .closing {
      animation: slideout 0.6s ease-in forwards;
    }
  </style>
</head>
<body>
  <div id="toast">
    <img src="https://lh3.googleusercontent.com/d/1VEpvGV-AiDWBemHLKW2Z7eXwuvWbO7vk" alt="Logo EVERCO" width="150">
    <span id="fecharToast" style="position: absolute; right: 40px; top: 20px; cursor: pointer; font-size: 20px;" onclick="fechar()"><b>x</b></span>
    <h2>Pausa Rápida</h2>
    <p>Ajuste sua postura, alongue o corpo e olhe para o horizonte por alguns segundos. <b>Seu bem-estar agradece!</b></p>
  </div>

  <script>
    function fechar() {
      window.close()
      
    }
    // Espera o DOM carregar
    window.onload = function() {
    
      setTimeout(fechar, 15000);
     
      window.resizeTo(460,220);
      window.moveTo(screen.availWidth-460, screen.availHeight-220);

    };
  </script>
</body>
</html>
